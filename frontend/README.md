# Frontend (React + Vite + TypeScript + Fluent UI + MSAL)

Generated scaffold for a Spec Driven multi-agent app UI.

## Overview
This project bootstraps a React 18 + Vite 5 + TypeScript SPA using Fluent UI components and Azure AD authentication via MSAL. It includes basic routing, theming, authentication hook, protected route guard, and an opinionated folder structure for growth.

## Tech Stack
- React 18, Vite 5, TypeScript (strict)
- Fluent UI (@fluentui/react-components) with custom theme overrides
- MSAL (@azure/msal-browser / @azure/msal-react) for Azure AD auth
- React Router v6 for navigation
- ESLint + Prettier for lint/format

## Folder Structure (src)
See in repo; key areas: auth (MSAL), routing (routes + guard), layout (shell), theme (design tokens + theme), services (http client), hooks (custom hooks), pages (route level views).

## Environment Variables (.env)
Copy `.env.example` to `.env` and fill values:
```
VITE_AAD_CLIENT_ID=<app-client-id>
VITE_AAD_TENANT_ID=<tenant-id>
VITE_AAD_REDIRECT_URI=http://localhost:5173
```

Authority is derived as: `https://login.microsoftonline.com/${VITE_AAD_TENANT_ID}`.

## Quick Start
```bash
npm install
npm run dev
```
Open http://localhost:5173.

## Scripts
- dev: start dev server
- build: type-check then build
- preview: preview production build
- lint: run eslint
- typecheck: run TypeScript without emit
- format: run prettier write
 - test / test:watch: run Vitest unit tests (jsdom + Testing Library)

## Auth & Login Flow
`PublicClientApplication` is configured in `auth/authConfig.ts`. The app wraps with `MsalProvider`. `useAuth()` exposes `login`, `logout`, `user`, `isAuthenticated`, `loading` and supports a development bypass.

### Dev Auth Bypass
Set `VITE_AUTH_BYPASS=true` in `.env` to skip MSAL entirely. The app will behave as if a mock user is authenticated; the Login page becomes decorative.

### Login Page
Located at `pages/Login/Login.tsx`:
 - Modern Fluent UI card layout
 - Email & password (disabled unless bypass on, since real auth is Microsoft sign-in)
 - Primary Login button (form submit / no-op under real MSAL; triggers navigate under bypass)
 - Secondary "Sign in with Microsoft" button invoking `login()`
 - Links: Forgot Password? / Sign Up (placeholders)
 - Redirects to `/dashboard` if already authenticated

### Protected Routes
`ProtectedRoute` now redirects unauthenticated users to `/login` instead of immediately launching the popup. After successful sign-in, future enhancement could read `location.state.from` to navigate back (state already stored). 

## Theming
`theme/createTheme.ts` clones Fluent light theme and overrides brand colors; `theme/tokens.ts` centralizes design tokens. Root provider uses `<FluentProvider theme={appTheme}>`.

## Adding Pages
Create folder under `pages/<PageName>/<PageName>.tsx` and add lazy import in `routing/routes.tsx`.

## Services
Use `services/httpClient.ts` for fetch wrapper (aborts + JSON). Add feature services returning typed promises (avoid direct fetch in components/pages).

## Testing
Vitest + @testing-library/react are configured in `vite.config.ts` (`test` block) with global `jest-dom` matchers via `vitest.setup.ts`.
Sample tests included:
 - `pages/Login/Login.test.tsx`
 - `routing/ProtectedRoute.test.tsx`

Run: `npm test` or watch: `npm run test:watch`.

## Next Ideas
- Add MSAL redirect flow handling and post-login redirect to prior route
- Add accessibility audits
- Integrate React Query for data fetching

## Attribution
Generated by AI as directed by Aaron King on 2025-08-26
